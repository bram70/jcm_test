<h1>Categories index</h1>
<%= button_to "Add new category", categories_new_path, method: :get %>
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category)%></li>
  <%end%>
</ul>
